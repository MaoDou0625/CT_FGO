# 60-80s 精度改进实验计划（按优先级）

## 0. 目标与统一评估口径
- 主目标：降低 60-80s 段在“运动->静止”过渡期的定位与速度误差，同时不牺牲全时段稳定性。
- 统一指标：
  - `Pos2D_RMSE`, `Pos2D_MAE`, `Pos2D_Max`
  - `Speed2D_RMSE`, `Speed2D_MAE`
  - 分段指标：`0-20, 20-40, 40-60, 60-80, 80-90s`
  - 收敛与代价：优化耗时、迭代次数、终止原因
- 接受门槛（初版）：
  - 60-80s `Pos2D_RMSE` 相对基线下降 >= 15%
  - 非目标时段（其余分段）`Pos2D_RMSE` 不恶化超过 5%
  - 速度指标不显著劣化（`Speed2D_RMSE` 增幅 < 5%）

## P0（最高优先）先把评估体系做稳定

### P0-1 分段批量评估自动化
- 动作：统一输出各时间段指标和总表（txt/csv），支持一键对比不同配置。
- 产出：`segment_benchmark` 结果目录、分段指标表、版本对比表。
- 风险：低。
- 退出条件：同一结果重复运行统计一致（数值波动在浮点误差范围内）。

### P0-2 残差/状态诊断增强
- 动作：按因子组（GNSS、Wheel Speed、NHC、IMU）导出每轮残差统计，记录 NO_CONVERGENCE 场景。
- 产出：每轮残差日志与可视化。
- 风险：低到中。
- 退出条件：可定位“谁主导误差”而非只看最终 RMSE。

## P1（中高优先）针对 60-80s 的模型/参数改进

### P1-1 权重敏感性扫描（先粗后细）
- 动作：对 `speed_weight / nhc_weight / attitude_weight_roll / attitude_weight_yaw` 网格扫描。
- 产出：Pareto 图（位置误差、速度误差、耗时）。
- 风险：中（实验量大）。
- 退出条件：找到稳定参数区间（不是单点最优）。

### P1-2 鲁棒核策略
- 动作：对 wheel/gnss 相关因子尝试 Huber/Cauchy，比较过渡段抗异常能力。
- 产出：鲁棒核开关实验报告。
- 风险：中。
- 退出条件：60-80s 改善且全局不退化。

### P1-3 过渡段自适应约束（运动->静止检测）
- 动作：基于速度与轮速一致性，动态降低可疑约束权重，避免过渡段“拉扯”。
- 产出：含触发日志的策略版配置。
- 风险：中到高（规则设计影响大）。
- 退出条件：在多个片段均提升，不仅单次数据有效。

## P2（中优先）时延与可观测性方向

### P2-1 IMU/GNSS 时间偏置再验证
- 动作：固定偏置回放 vs 联合估计，比较 60-80s 动态段速度一致性。
- 产出：时延假设对比表。
- 风险：中。
- 退出条件：明确“时延是否一阶主因”。

### P2-2 外参与轮模型可观测性检查
- 动作：统计外参、轮半径、姿态相关参数在过渡段的信息量/协方差变化。
- 产出：参数可观测性报告。
- 风险：中。
- 退出条件：明确哪些参数应固定先验，哪些可在线估计。

## 实施顺序（建议）
1. P0-1 分段评估自动化（先做）
2. P0-2 残差诊断增强
3. P1-1 权重扫描
4. P1-2 鲁棒核
5. P1-3 过渡段自适应约束
6. P2-1 / P2-2 作为机理验证与定型收尾

## 当前开始实施项
- 已启动：P0-1（分段批量评估自动化）。
